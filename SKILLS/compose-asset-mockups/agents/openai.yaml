interface:
  display_name: "Asset Mockup Composer"
  short_description: "Compose validated asset mockups and presentations"
  default_prompt: "Use $compose-asset-mockups to create a non-destructive, hash-backed presentation from validated game assets."
