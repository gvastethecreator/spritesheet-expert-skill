# Maintainer evidence

This directory keeps source-repository research and rejected visual candidates
outside every installable skill. Nothing here is a runtime dependency or an
approved production asset.

`motion-reference-candidates/` preserves historical Image Gen attempts for
maintainer comparison. Published template manifests may reference only
approved assets stored inside the selected skill with an exact SHA-256.
